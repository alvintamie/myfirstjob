%table{:class => "table"}
  %tr 
    %th Company Name
    %th Type
    %th Actions
  - @company_details.each do |company_detail|
    %tr
      %td= company_detail.company_name
      %td= company_detail.company_type
      %td
        = link_to "Edit My Company Profile", edit_employer_company_detail_path(company_detail), :class => "btn btn-primary"
        = link_to "Delete My Company Profile", employer_company_detail_path(company_detail), :method => :delete, data: { :confirm => "Are you sure?" }, :class => 'btn btn-danger'