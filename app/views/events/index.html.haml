.hero-unit8
  %ul.breadcrumb.navigation
    %li
      = link_to root_path do
        %i.icon-folder-open
        Home
    %span.divider >
    %li.gray
      Stories
%br
%br
.row
  .span8
    .hero-unit8
      %h1 Stories
      %p The latest news for cool undergraduates and professionals! Updated regularly with no boring option!
      %br
    .row
      - @events.each do |event|
        .article_box
          .article_image_box
            = image_tag event.image.url
          <div class="wrapper" id="snippets">
          %h1= link_to event.title, event_path(event)
          .green
            %p= event.created_at.strftime("%d %b %Y") + " by " + event.author
          = event.content
          </div>
    .pull-right
      = will_paginate @events
  .span4
    .hero-unit4
      %h1 About Stories
      .space
      %p We bring "Stories" updated everyday!
      .space
      %p - Explore all the gossips and what-you-have-to-know stuffs in Singapore. 
      .space
      %p - Find tips for undergraduates and professionals about life!
    .hero-unit4
      %h1 Top Stories
      - @featuredevents.each do |event|
        .space
        %p= link_to event.title, event_path(event)
      %br
      <script type="text/javascript"><!--google_ad_client = "ca-pub-7141414805566169";
      /* Homepage Ad */
      google_ad_slot = "2265020331";
      google_ad_width = 300;
      google_ad_height = 250;
      //-->
      </script>
      <script type="text/javascript" src="http://pagead2.googlesyndication.com/pagead/show_ads.js">
      </script>
      
      

